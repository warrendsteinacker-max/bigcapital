name: Deploy Webapp to Pages & Publish Server Container

on:
  push:
    branches:
      - develop

# Grants GitHub the permissions to write packages (for server container) AND deploy to GitHub Pages
permissions:
  contents: read
  packages: write
  pages: write
  id-token: write

# Allow only one concurrent deployment to Pages
concurrency:
  group: "pages"
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  SERVER_IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/server

jobs:
  # JOB 1: Build the Webapp and Deploy it to GitHub Pages
  deploy-webapp-pages:
    name: Build and deploy webapp to GitHub Pages
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      # Bypasses the frozen lockfile verification and ignores the Node engine mismatch warning
      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile --ignore-scripts

      # Uses the root monorepo build script to build the dependency packages and compile the webapp
      - name: Build Webapp with dependency chain
        run: pnpm run build:webapp
        env:
          VITE_BASE_URL: /${{ github.event.repository.name }}/

      - name: Setup Pages
        uses: actions/configure-pages@v4

      # Uploads Vite static build files from 'packages/webapp/dist'
      - name: Upload static website artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './packages/webapp/dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  # JOB 2: Build and push the Server backend container
  build-publish-server:
    name: Build and deploy server container
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker Server
        id: meta-server
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.SERVER_IMAGE_NAME }}
          tags: |
            type=raw,value=develop

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        id: build
        with:
          context: .
          file: ./packages/server/Dockerfile
          platforms: linux/amd64
          push: true
          labels: ${{ steps.meta-server.outputs.labels }}
          tags: ${{ steps.meta-server.outputs.tags }}

      - name: Export digest
        run: |
          mkdir -p /tmp/digests
          digest="${{ steps.build.outputs.digest }}"
          touch "/tmp/digests/${digest#sha256:}"

      - name: Upload digest
        uses: actions/upload-artifact@v4
        with:
          name: digests-server
          path: /tmp/digests/*
          if-no-files-found: error
          retention-days: 1
          
